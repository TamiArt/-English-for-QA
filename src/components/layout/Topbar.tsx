import { Flame, Menu, Settings } from 'lucide-react';

export const Topbar = ({ streak, email, onAccount }: { streak: number; email: string; onAccount: () => void }) => <header className="topbar"><button className="mobile-menu" aria-label="Открыть меню"><Menu size={20} /></button><span className="breadcrumb">Путь обучения <b>/</b> Обзор</span><div className="top-actions"><span className="streak"><Flame size={17} fill="currentColor" /> {streak} дня</span><button className="icon-button" aria-label="Настройки"><Settings size={18} /></button><button className="mini-avatar" type="button" onClick={onAccount} aria-label={email ? `Профиль ${email}` : 'Зарегистрироваться'}>{email ? email.slice(0, 1).toUpperCase() : 'A'}</button></div></header>;